package testapi.controller;


class TestPlanteController {
    /*

    @Test
    void testAjouterPlante() {
        // Mock du service
        PlanteService planteService = Mockito.mock(PlanteService.class);

        // Création du contrôleur avec le service mocké
        PlanteController planteController = new PlanteController(planteService);

        // Données de test
        String planteJson = """
                {
                    "id": 0,
                    "nom": "Tournesol",
                    "pointDeVie": 100,
                    "attaqueParSeconde": 0.00,
                    "degatAttaque": 0,
                    "cout": 50,
                    "soleilParSeconde": 2.00,
                    "effet": "normal",
                    "cheminImage": "path/image_tournesol.jpeg"
                }
                """;

        // Appel de la méthode du contrôleur
        ResponseEntity<String> response = planteController.ajouterPlante(planteJson);

        // Vérification du résultat
        assertEquals(200, response.getStatusCodeValue());
    }
    */
}